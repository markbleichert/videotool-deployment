{
  "name": "videotool",
  "version": "0.0.0",
  "groupId": "nl.rabobank.dist.online",
  "description": "Videotool deployment bundle",
  "scripts": {
    "build": "node build.js",
    "publish": "npm run build && tnl-publish --type project"
  },
  "devDependencies": {
    "@tnl/tnl-publish": "latest",
    "fs-extra": "^7.0.1"
  },
  "dependencies": {}
}
